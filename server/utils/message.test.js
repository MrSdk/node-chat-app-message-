const {generateMessage} = require("./message.js")
const expect = require("expect")
const request = require("supertest");

  // describe('POST /todos',()=>{

  // describe('Change Message',()=>{
    it('should return message with createdAt',()=>{
        var user = {
            from: "User",
            text: "Hello developer"
        }
            expect()
    })
// })
